const PORT = 2137;
const DEFAULT_POLLING_INTERVAL = 10000; // 10 seconds
const DEFAULT_TIME_PER_FOLLOWER = 30000; // 30 seconds
const PLATFORM_NAMES = ['youtube', 'tiktok', 'instagram', 'facebook'];

module.exports = {
  PORT,
  DEFAULT_POLLING_INTERVAL,
  DEFAULT_TIME_PER_FOLLOWER,
  PLATFORM_NAMES,
};
